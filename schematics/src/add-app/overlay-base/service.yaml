apiVersion: v1
kind: Service
metadata:
  name: <%= appName %>
  labels:
    app: <%= appName %>
spec:
  selector:
    app: <%= appName %>
  ports:
    - port: <%= appPort %>
      name: http-<%= appPort %>
